# Track A (Spark) only. Kept separate from requirements.txt so the frozen
# reproduction environment in .venv is never modified by this branch.
pyspark==3.5.3
pyarrow==17.0.0
pandas==2.2.3
pytest==8.3.3
pyyaml==6.0.2
